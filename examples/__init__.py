"""Example scripts demonstrating SBOM analysis functionality."""

from pathlib import Path
import sys

# Add the parent directory to Python path if running examples directly
if __name__ == "__main__":
    sys.path.append(str(Path(__file__).parent.parent)) 